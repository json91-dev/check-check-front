import React from 'react';
import {SafeAreaView, SafeAreaViewComponent, StyleSheet, Text} from 'react-native';

const OnBoardPage1 = () => {
  return (
    <SafeAreaView>
      <Text>Page</Text>
    </SafeAreaView>
  )
};

export default OnBoardPage1;

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    width: '100%',
    height: '100%',
  },

});
